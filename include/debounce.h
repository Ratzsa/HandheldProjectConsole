#ifndef __BUTTON_DEBOUNCING_H
#define __BUTTON_DEBOUNCING_H

#include <stdint.h>
#include <stdio.h>

// void dummyfunc();

#define DEBOUNCE 5

//uint16_t buttonsMovement[] = { DPAD_LEFT_Pin, DPAD_RIGHT_Pin, DPAD_UP_Pin, DPAD_DOWN_Pin, BTN_SHL_Pin }
////uint8_t buttonsAction = { BTN_RED_Pin, BTN_GRN_Pin, BTN_YEL_Pin }
//
//#define NUMBUTTONSMOVE sizeof(buttonsMovement)
//// #define NUMBUTTONSACTION sizeof(buttonsAction)
//
//uint8_t pressed[NUMBUTTONSMOVE];
//uint8_t justReleased[NUMBUTTONSMOVE];
//uint8_t justPressed[NUMBUTTONSMOVE];

//uint8_t pressed[NUMBUTTONSACTION];
//uint8_t justReleased[NUMBUTTONSACTION];
//uint8_t justPressed[NUMBUTTONSACTION];



#endif
